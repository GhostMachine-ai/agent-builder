interface:
  display_name: "Agent Builder"
  short_description: "Repo-specific patterns and workflows for agent-builder"
  default_prompt: "Use the agent-builder repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true